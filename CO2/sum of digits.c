#include<stdio.h>
int main()
{
	int n, i, count;
	printf("Enter a number");
	scanf("%d", &n);
	i=n;
	count=0;
	while(i>0)
	{
		count++;
		i=i%10;
		j=i
	}
	printf("Number of digits is %d", &count);
	}
}
