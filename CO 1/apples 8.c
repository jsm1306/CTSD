#include<stdio.h>
int main()
{
	int x, y, apple, mango;
	printf("Enter the cost of apples"); // per dozen
	scanf("%d", &apple);
	printf("Enter the cost of mango"); // per 3 mangoes
	scanf("%d", &mango);
	rateA = 12*apple;
	rateM = 3*mango;
	if (rateA>rateM)
	
}
