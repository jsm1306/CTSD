#include<stdio.h>
int main()
{
	int marks, total, marks_in_maths, marks_in_physics, marks_in_chemistry;
	printf("Enter your marks");
	scanf("%d", &marks);
	total=marks_in_maths+marks_in_physics+marks_in_chemistry
	if(marks_in_maths>=65 && marks_in_physics>=55 && marks_in_chemistry>=50)
	{
		if(total_marks>=190 || (marks_in_maths + marks_in_physics>=140))
		printf("ELIGIBLE");
	} else
	{
		printf("NOT ELIGIBLE");
	}
}
